# 導入事例サイト（case.airpangaea.com）実装ルール

## このリポジトリの目的

承認済みの導入事例ページと、その一覧（索引ページ）を `case.airpangaea.com` で公開する。
最初の1本（徳島北、2026-09-15 公開）に続き、他校の事例も1本ずつ追加していく。
テンプレート化・事例の追加は、公開済みのページを壊さない範囲で行う。汎用化のために原本の見た目や文面を変えない。

---

## 絶対に守ること

### 1. 本文を書き換えない

`source/<slug>.html` は、学校（自治体案件では教育委員会も）の掲載承認を受けた文面そのもの。凍結し、編集しない。
`source/tokushima-kita-2025.html` は、徳島県教育委員会 高校教育課および徳島県立徳島北高等学校の掲載承認を受けたもの。

- 本文、見出し、数値、アンケートの設問文、有効回答数は一字も変更しない
- 誤字・表記ゆれに見えるものがあっても直さない。報告して判断を待つ
- テンプレートから生成した出力の本文テキストが、原本と差分ゼロであることを `scripts/verify.py` で確認できる状態にする

### 2. 金額・契約方式を書かない

受託金額、契約形態、単価に類する情報は一切載せない。原本に書かれていないのは意図的な判断による。

### 3. 画像は原本に同梱されたものだけ

各事例ページは、その原本に base64 で埋め込まれた画像だけを使う（徳島北は4点：JPEG 3点・PNG 1点）。

- 追加、差し替え、ストック写真での補完は不可
- 生徒が作成した資料やスライドは著作権上の理由で使用できない
- 顔が判別できる写真は使用しない
- 実装では base64 を `public/images/<slug>/` に切り出す。切り出し以外の加工（トリミング、色調整、見た目が変わる再圧縮）はしない
- `public/images/<slug>/` はビルドで再生成しない。消さない（消したら `git restore`）

### 4. 印刷（A4・PDF）で崩さない

自治体の担当者はページを印刷して回覧する。以下は原本で解決済みだが、テンプレート化で壊れやすい。

- `@page { margin: 16mm }` はメディアクエリの外に置く。`@media print {}` の中に入れると効かない
- 背景色だけで意味を持つ要素を作らない。ブラウザは印刷時に背景色を落とす。アンケートのバーは同色の枠線を重ねてある。白抜き文字のボタンは印刷時に枠線＋濃色文字へ切り替える
- 幅は `rem` ではなく実寸（170mm）で指定する。`rem` のままだと A4 の版面をはみ出す
- 改ページ禁止は小さい塊だけに限定する。大きい塊に付けるとページ下部に大きな空白が出る

レイアウトに触れたら、必ず A4 の印刷プレビューで確認する。

### 5. 掲載承認のない事例を載せない

事例ページの本文・写真・生徒の声は、掲載承認を受けたものだけを載せる。

| 区分 | 扱い |
|---|---|
| 自治体案件（教育委員会が関わる） | 教育委員会と学校の正式な掲載承認が必須 |
| プレスリリースで公表済みの事実 | 公表済みの範囲なら再掲できる。ただし学校へ一報を入れてから |
| 未公表の内容、写真、生徒の声 | 学校の掲載承認が必須 |

承認が取れるまで `source/` に原本を置かない。原本に無い事実を、受領資料やメールから補わない。

---

## 事例ごとの状況

| 事例 | 状況 | 次にやること |
|---|---|---|
| 徳島北（`tokushima-kita-2025`） | 公開済み（2026-09-15） | — |
| 東京女子 | 未着手。原稿は既存ページの型に合わないが、型は障害にしない | 学校へ掲載許諾を打診 |
| 西大和学園 | 2023.11 のプレスリリースを一覧の「最新事例」に掲載中 | リリース時に学校の了解を得ていたか確認し、確認メールを送る |
| HR高等学院 | 保留（リリースの「5カ国」が実績では2カ国、今年度の実施が未確定） | 実施状況が固まってから判断 |
| MOTTAINAI 4本（明星×Padang、成蹊・武蔵×BBS、成蹊×BBS、ECoN×Mapúa） | Wix に掲載のまま。一覧からは外部リンク | 当面そのまま |

状況が変わったらこの表を更新する。承認を得たら、事例ごとに別ブランチで作業し、プレビューで確認をもらってから main に入れる。

---

## 見た目の仕様

### 事例ページ（原本に実装済み・変更しない）

- 配色：紺 `#16283D` を基調。アンケートのバーは `#3480AE`
- 書体：見出し Noto Serif JP、本文 Noto Sans JP
- 社名は `AirPangaea`。2語に分けない

### ヘッダー・フッター（全ページ共通。Wix の導入事例ページに合わせる）

ヘッダー・フッター・`<head>` はサイト共通の枠で、指示により変更してよい。1ページだけ直さず、必ず全ページ（事例ページ・一覧）で揃える。

- ヘッダー：`ja.airpangaea.com/case` と同じ白帯。ロゴ左（表示幅 215px）、メニュー中央（ホーム／プログラム／導入事例／会社概要／Blog、Raleway 14px、現在地 `#2E58FF`）。ログイン・日本語は置かない
- ロゴは 500×112px の PNG。Wix の配信 URL を直接参照しない
- フッター：`©2021-2026 AirPangaea, Inc.` ＋ プライバシーポリシー／利用規約／特定商取引法に基づく表記 ＋ SNS アイコン（Instagram・X・Facebook。X は `https://x.com/airpangaeajapan`、ロゴは現行の X）
- favicon は Wix と同じ画像（`public/images/favicon/`）
- サイトの外に出るリンク（http/https）はすべて新しいタブで開く（`target="_blank" rel="noopener noreferrer"`。`build.py` の `link_attrs()`）
- お問い合わせは `https://ja.airpangaea.com/#contactus`

---

## 原本との突き合わせ（`scripts/verify.py`）

`source/<slug>.html` ごとに `public/<slug>/index.html` と比べる。

1. 本文テキスト：文書全体（ヘッダー・フッター含む）のテキストと、alt・aria-label・href・meta content などの属性値
2. 画像：`public/images/<slug>/` の画像が原本の base64 とバイト単位で同一で、余分な画像がないこと（favicon・SNS アイコンなど共通の画像は対象外）
3. 本文 HTML：`<main>…</main>` の中が原本とバイト単位で一致すること（画像の src を除く）

ヘッダー・フッター・`<head>` を指示で変えたら、チェック1が差分を出す。そのときは `APPROVED_CHANGES` に日付と指示内容を添えて登録する。`<main>` の中の差分は登録で逃がさない。

---

## 公開先

- Cloudflare Pages（GitHub `airpangaea/airpangaea-case` の main を公開。出力ディレクトリ `public`、ビルドコマンドなし）
- ドメインは `case.airpangaea.com`（`airpangaea.com` のサブドメイン。新規ドメインは取得しない）
- `tomodachi-english.airpangaea.com`（`tden-lp`）とは別リポジトリ・別プロジェクト
- 公開 URL は `https://case.airpangaea.com/<slug>/`。一覧は `https://case.airpangaea.com/`

---

## フォルダ構成

```
airpangaea-case/
├── CLAUDE.md
├── source/
│   └── <slug>.html                       承認済み原本。凍結し、編集しない
├── content/
│   ├── index.md                          事例一覧（最新事例・プログラム見出し＋事例の並び・生徒たちの声）
│   ├── <slug>.md                         front matter ＋ 本文
│   └── external/                         Wix に残っている事例のカード（front matter のみ）
├── templates/
│   ├── case.html                         事例ページ
│   └── index.html                        事例一覧
├── scripts/
│   ├── build.py                          content/ ＋ templates/ → public/
│   └── verify.py                         公開用 HTML と承認済み原本の突き合わせ
└── public/                               Cloudflare Pages の出力ディレクトリ
    ├── index.html                        事例一覧
    ├── <slug>/
    │   └── index.html
    └── images/
        ├── <slug>/                       原本から切り出した画像（ビルドで再生成しない）
        ├── index/                        一覧のヒーロー・SDGs ロゴ・EducationLink ロゴ
        ├── external/                     Wix 事例のカード写真
        ├── shared/                       SNS アイコン
        └── favicon/
```

固定するのは3点のみ。`source/` の凍結、`public/` が出力ディレクトリであること、公開 URL のパス。
テンプレートの構成方法とビルド手段は任せる。

front matter の項目は原本から読み取って定義する。将来の事例を想定した項目を先回りで足さず、新しい事例で実際に必要になったときに足す。索引カードに出す値だけを front matter に置き、本文に表示される事実（設問文、有効回答数など）は本文側に置く。

---

## このリポジトリに入れないもの

クライアントから受領した資料（実績報告書、業務委託仕様書、教育委員会・学校とのメール）。
公開してよい内容は承認済みの原本 HTML に含める。原本に無い事実を資料から補わない。

---

## このリポジトリでやらないこと

- Wix 側の編集（`/case` から新サイトへの誘導など）。手作業で行う

---

## 事例一覧（索引ページ `public/index.html`）

事例ページとは別に、見た目は Wix の導入事例ページ（`https://ja.airpangaea.com/case`）に合わせる。Wix のトップや他のページから移動してきても違和感がないことを優先する。

- 構成：写真のヒーロー（紺のオーバーレイ）→「最新事例」（控えめなテキストリンク）→ プログラムごとの事例カード →「生徒たちの声」。「生徒たちの成果事例」は載せない
- 文言：事例ページのカードは front matter、Wix の事例・最新事例・生徒たちの声は Wix `/case` に掲載中の文言をそのまま使う。見やすくするための分割や見出し付けはよいが、文面は変えない
- 写真：事例ページのカードはその事例の同梱画像から使う。Wix の事例は Wix に掲載中の写真を取得してリポジトリに置く（Wix の配信 URL を直接参照しない）
- EducationLink：Wix で「企画協力」として EducationLink のロゴが出ている事例には、必ず同じロゴを付ける。外さない
- `<head>`：meta description は `content/index.md` の front matter。Bing Webmaster Tools の確認タグ（msvalidate.01）は消さない
- 一般募集プログラムの卒業ページへのリンクは後日検討（未着手）
