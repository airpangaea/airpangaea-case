# 徳島北高校 導入事例ページ 実装ルール

## このリポジトリの目的

承認済みの導入事例ページ1本を `case.airpangaea.com` で公開する。
テンプレート化は、この1本を壊さない範囲で行う。汎用化のために原本の見た目や文面を変えない。

---

## 絶対に守ること

### 1. 本文を書き換えない

`source/tokushima-kita-2025.html` は、徳島県教育委員会 高校教育課および徳島県立徳島北高等学校の掲載承認を受けた文面そのもの。

- 本文、見出し、数値、アンケートの設問文、有効回答数は一字も変更しない
- 誤字・表記ゆれに見えるものがあっても直さない。報告して判断を待つ
- テンプレートから生成した出力の本文テキストが、原本と差分ゼロであることを確認できる状態にする

### 2. 金額・契約方式を書かない

受託金額、契約形態、単価に類する情報は一切載せない。原本に書かれていないのは意図的な判断による。

### 3. 画像は同梱の4点のみ

原本に base64 で埋め込まれた画像4点（JPEG 3点・PNG 1点）だけを使う。

- 追加、差し替え、ストック写真での補完は不可
- 生徒が作成した資料やスライドは著作権上の理由で使用できない
- 顔が判別できる写真は使用しない
- 実装では base64 を外部ファイルに切り出す。切り出し以外の加工（トリミング、色調整、見た目が変わる再圧縮）はしない

### 4. 印刷（A4・PDF）で崩さない

自治体の担当者はページを印刷して回覧する。以下は原本で解決済みだが、テンプレート化で壊れやすい。

- `@page { margin: 16mm }` はメディアクエリの外に置く。`@media print {}` の中に入れると効かない
- 背景色だけで意味を持つ要素を作らない。ブラウザは印刷時に背景色を落とす。アンケートのバーは同色の枠線を重ねてある。白抜き文字のボタンは印刷時に枠線＋濃色文字へ切り替える
- 幅は `rem` ではなく実寸（170mm）で指定する。`rem` のままだと A4 の版面をはみ出す
- 改ページ禁止は小さい塊だけに限定する。大きい塊に付けるとページ下部に大きな空白が出る

レイアウトに触れたら、必ず A4 の印刷プレビューで確認する。

---

## 見た目の仕様（原本に実装済み・変更しない）

- 配色：紺 `#16283D` を基調。アンケートのバーは `#3480AE`
- 書体：見出し Noto Serif JP、本文 Noto Sans JP
- ヘッダー・フッターは `ja.airpangaea.com` の白帯部分のみ。メニューはホーム／プログラム／導入事例／会社概要／Blog、右端にログイン・日本語
- ロゴは 500×112px の PNG を表示幅 200px。Wix の配信 URL を直接参照しない
- フッター：`©2021-2026 AirPangaea, Inc.` ＋ プライバシーポリシー／利用規約／特定商取引法に基づく表記
- 社名は `AirPangaea`。2語に分けない

---

## 公開先

- Cloudflare Pages
- ドメインは `case.airpangaea.com`（`airpangaea.com` のサブドメイン。新規ドメインは取得しない）
- `tomodachi-english.airpangaea.com`（`tden-lp`）とは別リポジトリ・別プロジェクト
- 公開 URL は `https://case.airpangaea.com/tokushima-kita-2025/`

---

## フォルダ構成

```
airpangaea-case/
├── CLAUDE.md
├── source/
│   └── tokushima-kita-2025.html          承認済み原本。凍結し、編集しない
├── content/
│   └── tokushima-kita-2025.md            front matter ＋ 本文
├── templates/
└── public/                               Cloudflare Pages の出力ディレクトリ
    ├── tokushima-kita-2025/
    │   └── index.html
    └── images/
        └── tokushima-kita-2025/
```

固定するのは3点のみ。`source/` の凍結、`public/` が出力ディレクトリであること、公開 URL のパス。
テンプレートの構成方法とビルド手段は任せる。

front matter の項目は原本から読み取って定義する。事例が1本しかない段階なので、将来の事例を想定した項目を先回りで足さない。索引カードに出す値だけを front matter に置き、本文に表示される事実（設問文、有効回答数など）は本文側に置く。

---

## このリポジトリに入れないもの

クライアントから受領した資料（実績報告書、業務委託仕様書、徳島県教育委員会とのメール）。
公開してよい内容は既にすべて原本 HTML に含まれている。原本に無い事実を資料から補わない。

---

## 今回やらないこと

- 索引ページの本実装
- 他校の事例ページ
- Wix `/case` の置き換え

徳島北1本の公開を先行させる。Wix 側の編集は手作業で行うため、このリポジトリのスコープ外。
